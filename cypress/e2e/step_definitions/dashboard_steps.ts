import { Given, Then, When } from "@badeball/cypress-cucumber-preprocessor";
import { Dashboard } from "../pages/dasbhoard";
const dashboardPage = new Dashboard();

Given("I navigate to the Dashboard Page", function () {});

Then("I should see the logo displayed on the page", function () {});

Then("I should see the login button displayed on the page", function () {});
